.container-fluid.grey(anchormove)
  .container.white
    .col-lg-11
      a.anchor#Overview
      center
        h3.text-red - {{project.project.typus}} -
        input.form-control#newProject_name(ng-model="edit.form.project.name", style="width: 100%;", placeholder="Der Titel deines Projektes")
        br
        a.blue.text-white.label.clickable(ng-repeat="tag in project.project.tags", style="margin-left: 5px;", ui-sref="root.community({id: tag.id})" target="_blank") {{tag.name}}
      br
      center
        input#newProject_cover_image(type="file", ngf-select, ng-model="edit.form.project.image" name="image" accept="image/*" ngf-max-size="1MB")
        label(for="newProject_cover_image") Hier kannst du dein Titelbild ändern. Max. 1MB.

      a.anchor#Ziel
      h2.text-blue Pitch
      hr.border-grey
      textarea#newProject_goal(ng-keyup="create.charLimit()", ng-change="create.charLimit()", ng-model="edit.form.project.goal", style="width: 100%; resize: none;", rows="2", maxlength="200", placeholder="Eine kurze Beschreibung des Projektziels; was willst du erreichen?")
      label(for="newProject_goal") {{create.pitch_characters}} verbleibend.
      br(clear="all")

      a.anchor#Problem
      div(ng-show="edit.form.project.typus != 'Open Innovation'")
        h2.text-blue Problem
        hr.border-grey
        textarea#newProject_problem(ng-disabled="edit.form.project.typus == 'Open Innovation'", ng-model="edit.form.project.problem", style="width: 100%; min-height: 10em; resize: vertical;", placeholder="{{create.problemPlaceholder}}")
        br(clear="all")

      a.anchor#Solution
      div(summernote)
        h2.text-blue Lösung
        hr.border-grey
        div#summernote(style="margin-top: 20px;")
      br(clear="all")
      a.anchor#Coop
      //h2.text-blue.text-center(ng-show="project.project.coop") Kooperationswunsch
      //hr(style="border-color: #b2b8cf;")
      //p {{project.project.coop_type}}
      //br(clear="all")
      hr(style="border-color: #b2b8cf;")
      a.anchor#Attachment
      div(ng-hide="project.project.attachment.url")
        h3.text-red Anhang anfügen
        center
          input#editProject_attachment(type="file", ngf-select, ng-model="edit.form.project.attachment" name="attachment" accept="application/pdf" ngf-max-size="2MB")
          label(for="editProject_attachment") Hier kannst Du noch ein PDF mit zusätzlichen Informationen anhängen. (max. 2MB)
      center
        button.btn.btn-lg.btn-info(ng-click="edit.editProject()") Speichern